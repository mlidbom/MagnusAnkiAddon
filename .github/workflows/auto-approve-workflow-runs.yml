name: Auto Approve Workflow Runs

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    steps:
      - uses: mheap/automatic-approve-action@851272e
        with:
          token: ${{ secrets.AUTO_APPROVE_WORKFLOWS_PERSONAL_ACCESS_TOKEN }}
